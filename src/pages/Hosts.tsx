// Hosts — placeholder for the "Become a host" editorial pitch page.
export default function Hosts() {
  return (
    <main id="main" className="px-gutter py-shelf">
      <h1 className="font-serif text-step-5 text-ink">Become a host</h1>
      <p className="mt-4 font-sans text-step-0 text-ink-soft">
        Coming soon — slice 4.3 will ship the hosts charter page.
      </p>
    </main>
  )
}
